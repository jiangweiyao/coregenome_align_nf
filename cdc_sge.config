process.executor = "sge"
process.queue = "all.q"
process.penv = 'smp'
singularity.runOptions = "--bind /scicomp/reference --bind /scicomp/groups --bind /scicomp/home --bind /scicomp/scratch"

executor.queueSize = 100
env._JAVA_OPTIONS="-Xms1024m -Xmx10240m"
