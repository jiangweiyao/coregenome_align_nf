process.executor = "sge"
process.queue = "all.q@@bigsky"
process.penv = 'smp'
singularity.runOptions = "--bind /scicomp/reference --bind /scicomp/groups --bind /scicomp/home"

executor.queueSize = 100

